/* ==================== Comentários ==================== */
// var y = 0 - comentário de linha (CRTL + ;)
/* var x =1 - comentário de várias linhas (SHIFT + ALT + A)
var z = 2*/

/* ==================== Variável ==================== */
var preco = 2
var desconto = 0.2

preco = preco - desconto //Quando trabalhamos com variáveis podemos dar outro valor a mesma

/* ==================== Constante ==================== */
const PRECO = 2 //boa prática escrever const com letra maiúscula
var desconto = 0.2

var total = console.log(PRECO - desconto) // solução para alterar o valor

PRECO = preco - desconto //Quando trabalhamos com constante, o valor não pode ser manipulado
